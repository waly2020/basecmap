const ButtonLoader = ({className}) => {
    return (
        <div className={`${className} bg-black py-3 px-2`}>
            <button className="text-white text-center w-full">Sign up now</button>
        </div>
    );
}

export default ButtonLoader;